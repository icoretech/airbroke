{
  "$schema": "https://react.doctor/schema/config.json",
  "lint": true,
  "deadCode": true,
  "noScore": true,
  "blocking": "error",
  "serverAuthFunctionNames": ["requireAuth", "requireUserId"],
  "ignore": {
    "overrides": [
      {
        "files": ["components/ui/**", "components/ai-elements/**"]
      }
    ]
  }
}
