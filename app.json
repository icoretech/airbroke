{
  "name": "airbroke",
  "description": "Open Source Error Catcher (Airbrake/Sentry-compatible) built with Next.js and Postgres.",
  "repository": "https://github.com/icoretech/airbroke",
  "keywords": ["nextjs", "postgres", "airbrake", "sentry", "error-tracking"],
  "website": "https://github.com/icoretech/airbroke",
  "scripts": {
    "postdeploy": "yarn db:migrate"
  },
  "env": {
    "BETTER_AUTH_SECRET": {
      "description": "Better Auth secret used to sign cookies and tokens.",
      "generator": "secret"
    },
    "BETTER_AUTH_URL": {
      "description": "Your app's public URL for OAuth callbacks (e.g. https://your-app.herokuapp.com).",
      "required": true
    },
    "NEXT_SERVER_ACTIONS_ENCRYPTION_KEY": {
      "description": "Optional: set this if you run multiple dynos/instances so Server Actions encryption is consistent across servers.",
      "generator": "secret",
      "required": false
    },
    "AIRBROKE_CORS_ORIGINS": {
      "description": "Comma-separated list of allowed origins for /api/v3/notices and /api/sentry/*/envelope CORS (or '*' for quick starts).",
      "value": "*",
      "required": false
    }
  },
  "addons": [
    {
      "plan": "heroku-postgresql"
    }
  ],
  "formation": {
    "web": {
      "quantity": 1
    }
  }
}
